# SAFE: this is UI code. The word "Select" is a label, not SQL.
def build_menu():
    options = ["Select an option", "Delete account", "Update profile"]
    return options

def show_prompt():
    return "Select from where you want to start"
