"""
Test corpus: 30+ vulnerable files covering every class the tool should catch.

Each file is intentionally minimal and clear so the AI has no excuse to miss it.
This is the GROUND TRUTH for the 10x test runs.
"""
